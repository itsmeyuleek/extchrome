<%= form_with(model: color_palette_widget, local: true) do |form| %>
  <% if color_palette_widget.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(color_palette_widget.errors.count, "error") %> prohibited this color_palette_widget from being saved:</h2>

      <ul>
      <% color_palette_widget.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :color %>
    <%= form.text_field :color %>
  </div>

  <div class="field">
    <%= form.label :type %>
    <%= form.number_field :type %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
