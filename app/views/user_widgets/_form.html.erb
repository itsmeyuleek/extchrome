<%= form_with(model: user_widget, local: true) do |form| %>
  <% if user_widget.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user_widget.errors.count, "error") %> prohibited this user_widget from being saved:</h2>

      <ul>
      <% user_widget.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.number_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :widgetable_type %>
    <%= form.text_field :widgetable_type %>
  </div>

  <div class="field">
    <%= form.label :widgetable_id %>
    <%= form.number_field :widgetable_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
